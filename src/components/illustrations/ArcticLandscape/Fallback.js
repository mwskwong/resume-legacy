import { Box } from "@mui/material";
import React from "react";

const Fallback = () => <Box sx={{ width: "100%", aspectRatio: "624.443 / 278.807" }} />;

Fallback.whyDidYouRender = true;

export default Fallback;