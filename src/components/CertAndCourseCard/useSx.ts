import { UseSx } from "types";

const useSx: UseSx = () => ({
  cardContent: {
    display: "flex",
    alignItems: "center"
  },
  icon: {
    mr: "16px"
  },
  status: {
    color: "text.secondary"
  }
});

export default useSx;