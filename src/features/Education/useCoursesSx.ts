import { UseSx } from "types";

const useSx: UseSx = () => ({
  title: {
    typography: "subtitle2",
    color: "primary.main",
    textAlign: "center"
  },
  card: {
    bgcolor: "background.sectionSecondary"
  }
});

export default useSx;