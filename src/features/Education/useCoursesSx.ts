import { UseSx } from "types";

const useSx: UseSx = () => ({
  title: {
    typography: "subtitle2",
    color: "primary.main",
    textAlign: "center"
  },
  cardContentStack: {
    alignItems: "center"
  }
});

export default useSx;