import { UseSx } from "types";

const useSx: UseSx = () => ({
  actionContainer: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    mb: 2
  }
});

export default useSx;